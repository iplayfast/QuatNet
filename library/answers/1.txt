import math

def compute_pi():
    pi_approx = 0.0
    sign = 1.0
    denominator = 1.0
    
    for _ in range(1000000):
        pi_approx += sign / denominator
        sign *= -1
        denominator += 2.0
    
    return 4 * pi_approx

print(compute_pi())
