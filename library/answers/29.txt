Here is an example of how your Python function might look:

```python
def greet(name):
    return f'Hello, {name}!'

# Testing our function
print(greet('User'))
```
This function takes in one parameter `name` and returns a greeting for the person named. For instance if you call this function with 'User', it will print out "Hello, User!".
