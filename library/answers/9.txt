Here's a simple Python function that takes two numbers as input and returns their sum:

```python
def add_numbers(a, b):
    return a + b
```
You can call this function by providing it with two numerical inputs like so:
`result = add_numbers(3, 5)` for example.
