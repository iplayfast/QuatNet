import random
n=random.randint(1,100)
guesses=set()
tries=0
while True:
    g=int(input('Guess: '))
    if g not in guesses: tries+=1
    guesses.add(g)
    if g<n: print('Too low')
    elif g>n: print('Too high')
    else: print(f'Correct in {tries} tries'); break