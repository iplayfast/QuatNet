def compute_pi(terms=1_000_000):
    pi = 0.0
    sign = 1
    for i in range(terms):
        pi += sign / (2 * i + 1)
        sign = -sign
    return 4 * pi

print(f"Pi approximation: {compute_pi()}")
