```python
def factorial(n: int) -> int:
    """
    Calculates the factorial of a non-negative integer n (n!).

    Args:
        n: The non-negative integer.

    Returns:
        The factorial of n.

    Raises:
        ValueError: If n is negative.
    """
    if not isinstance(n, int):
        raise TypeError("Input must be an integer.")
    if n < 0:
        raise ValueError("Factorial is not defined for negative numbers.")
    if n == 0:
        return 1
    
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result

if __name__ == '__main__':
    # Example usage 1: Standard case
    num1 = 5
    print(f"The factorial of {num1} is: {factorial(num1)}")  # Output: 120

    # Example usage 2: Edge case (0!)
    num2 = 0
    print(f"The factorial of {num2} is: {factorial(num2)}")  # Output: 1

    # Example usage 3: Larger number
    num3 = 10
    print(f"The factorial of {num3} is: {factorial(num3)}") # Output: 3628800

    # Example of error handling (uncomment to test)
    # try:
    #     factorial(-1)
    # except ValueError as e:
    #     print(f"Error caught: {e}")
```
