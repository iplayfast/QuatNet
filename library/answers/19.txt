def fuzzy_match(pattern, text):
    pi, ti = 0, 0
    while pi < len(pattern) and ti < len(text):
        if pattern[pi].lower() == text[ti].lower(): pi += 1
        ti += 1
    return pi == len(pattern)
