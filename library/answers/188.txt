```python
def max_subarray_sum(nums):
    """
    Finds the maximum sum of a contiguous subarray using Kadane's algorithm.

    Args:
        nums (list[int]): The input list of integers.

    Returns:
        int: The maximum subarray sum.
    """
    if not nums:
        return 0

    max_so_far = nums[0]
    current_max = nums[0]

    for i in range(1, len(nums)):
        num = nums[i]
        # The current maximum ending at index i is either the element itself,
        # or the element plus the previous current maximum.
        current_max = max(num, current_max + num)

        # Update the overall maximum sum found so far.
        max_so_far = max(max_so_far, current_max)

    return max_so_far

if __name__ == '__main__':
    # Test case 1: Standard positive/negative mix
    nums1 = [-2, 1, -3, 4, -1, 2, 1, -5, 4]
    result1 = max_subarray_sum(nums1)
    print(f"Input: {nums1}")
    print(f"Output: {result1}")  # Expected: 6 (subarray [4, -1, 2, 1])

    # Test case 2: All positive numbers
    nums2 = [1, 2, 3, 4, 5]
    result2 = max_subarray_sum(nums2)
    print(f"Input: {nums2}")
    print(f"Output: {result2}")  # Expected: 15

    # Test case 3: All negative numbers (The result should be the largest single element)
    nums3 = [-10, -2, -5, -1]
    result3 = max_subarray_sum(nums3)
    print(f"Input: {nums3}")
    print(f"Output: {result3}")  # Expected: -1

    # Test case 4: Single element
    nums4 = [7]
    result4 = max_subarray_sum(nums4)
    print(f"Input: {nums4}")
    print(f"Output: {result4}")  # Expected: 7

    # Test case 5: Empty list
    nums5 = []
    result5 = max_subarray_sum(nums5)
    print(f"Input: {nums5}")
    print(f"Output: {result5}")  # Expected: 0
```
