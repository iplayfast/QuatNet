def solve_problem():
    x = [1, 2, 3]
    y = ['a', 'b', 'c']
    
    z = [(i, j) for i in range(len(x)) for j in range(len(y))]
    print(z)
    result = []
    for t in z:
        if x[t[0]] * y[t[1]] > 5:
            result.append(t)
    return result

print(solve_problem())
