Sure, here's a Python function that returns the length of a string:

```python
def str_len(s):
    return len(s)
```

You can call it with any string like so:
```python
print(str_len("Hello, World!"))  # Outputs: 13
```
