Here is a simple Python function that takes two arguments and returns their sum:

```python
def add_numbers(a, b):
    return a + b
```
You can call it with any two numbers like so:
`print(add_numbers(5, 10)) # outputs: 15`

This solution meets the given problem by providing a Python function that adds together any two numerical inputs.
