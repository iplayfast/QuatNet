Here is your Python function:

```python
def my_function(a, b):
    return a + b

result = my_function(5, 10)
print(result)
```

As you didn't provide specifics about what the function should do or take in as parameters, I created a simple function that takes two arguments and returns their sum. The result is then printed out.

The `my_function` function takes in two parameters `a` and `b`. It simply adds these two numbers together and returns the resulting value.
