Sure, here is an example of a Python function:

```python
def greet(name):
    return f'Hello, {name}!'

print(greet('AI'))
```

This function takes in one parameter (`name`) and returns a greeting message including that name.
When we call the function with 'AI' as argument, it prints: "Hello, AI!"
