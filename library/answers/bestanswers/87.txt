import math

def nCr(n, r):
    if r < 0 or r > n: return 0
    r = min(r, n - r)
    result = 1
    for i in range(r):
        result = result * (n - i) // (i + 1)
    return result

def nPr(n, r):
    if r < 0 or r > n: return 0
    return math.prod(n - i for i in range(r))
