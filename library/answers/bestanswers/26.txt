import datetime

year = datetime.date.today().year
leap_years = []
while len(leap_years) < 20:
    year += 1
    if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
        leap_years.append(year)
print(f"Next 20 leap years: {leap_years}")
