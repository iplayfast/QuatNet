def progress_bar(frac, width=40):
    filled = int(frac * width)
    bar = '[' + '#' * filled + '-' * (width - filled) + ']'
    pct = int(frac * 100)
    print(f"\r{bar} {pct}%", end='', flush=True)
    if frac >= 1.0:
        print()
